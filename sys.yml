---
- name: Playbook for sys group
  hosts: 
    - "{{ host }}"
  become_user: root
  become: yes

  # pre_tasks:
  #   - name: Pre Task => Update Yum
  #     dnf: name=* state=latest

  roles:
    - openldap
    - dnsmasq
    - openldap-auth
    - nginx

  # post_tasks:
  #   - name: post tasks
  #     dnf: name=* state=latest
