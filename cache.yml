---
- name: Playbook for cache role
  hosts: 
    - cache
  become_user: root
  become: yes

  # pre_tasks:
  #   - name: Pre Task => Update Yum
  #     dnf: name=* state=latest

  roles:
    - redis
    - memcache
    - rabbitmq

  # post_tasks:
  #   - name: post tasks
  #     dnf: name=* state=latest
