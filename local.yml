---
- name: "Playbook for localhost"
  hosts: localhost
  connection: local
  become_user: root
  become: yes

  # pre_tasks:
  #   - name: Pre Task => Update Yum
  #     dnf: name=* state=latest
  roles: 
    - "{{ role_name }}"
  # post_tasks:
  #   - name: post tasks
  #     dnf: name=* state=latest
