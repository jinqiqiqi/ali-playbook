---
- name: Playbook for role php7-remi
  hosts: 
    - php7-remi
  become_user: root
  become: yes

  # pre_tasks:
  #   - name: Pre Task => Update Yum
  #     dnf: name=* state=latest

  roles:
    - php7-remi
    - nginx

  # post_tasks:
  #   - name: post tasks
  #     dnf: name=* state=latest
